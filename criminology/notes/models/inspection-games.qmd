---
title: "Inspection games (deterrence & monitoring)"
toc: true
---

## What it is
Inspection games model strategic interaction between:
- an agent who may violate (offend), and
- an enforcer who can monitor/inspect at a cost.

They formalize deterrence under scarce enforcement resources.

---

## Canonical setup
Offender chooses $C$ (crime) or $N$ (no crime).  
Enforcer chooses $I$ (inspect) or $\bar I$ (not inspect).

Parameters:
- illegal gain if successful: $b>0$
- sanction if caught: $s>0$
- harm to enforcer if crime succeeds: $h>0$
- inspection cost: $k>0$

Payoffs:
- If $N$: offender $0$; enforcer pays $-k$ if inspecting, $0$ otherwise.
- If $C$ and $I$: offender caught $\to -s$; enforcer pays $-k$.
- If $C$ and $\bar I$: offender succeeds $\to b$; enforcer suffers $-h$.

---

## Solved equilibrium (mixed strategies)
Let enforcer inspect with probability $q$ and offender commit crime with probability $p$.

### Step 1: offender indifference
$$
\mathbb{E}[u_O(C)] = q(-s) + (1-q)b = b - q(b+s).
$$
Indifference with $N$ (payoff 0) gives:
$$
q^*=\frac{b}{b+s}.
$$

### Step 2: enforcer indifference
Enforcer payoff from inspecting:
$$
\mathbb{E}[u_E(I)] = -k
$$
Enforcer payoff from not inspecting:
$$
\mathbb{E}[u_E(\bar I)] = -ph.
$$
Indifference yields:
$$
p^*=\frac{k}{h}.
$$

Interior mixing requires $0<k<h$.

---

## Predictions (comparative statics)
- $q^*$ decreases in $s$ and increases in $b$.
- $p^*$ increases in $k$ and decreases in $h$.

---

## Connections
### Mechanisms
- Incentives & deterrence → `../mechanisms/incentives-deterrence.qmd`
- Opportunity & routine activity (affects $b$ and effective $q$) → `../mechanisms/opportunity-routine.qmd`
- Legitimacy & compliance (endogenous detection/reporting) → `../mechanisms/legitimacy-compliance.qmd`
- Self-control (perceived risk, present bias) → `../mechanisms/self-control.qmd`

### Institutions
- Policing (monitoring allocation) → `../institutions/policing.qmd`
- Corrections (supervision as monitoring) → `../institutions/corrections.qmd`

### Foundations
- Recorded vs incidence (detection/reporting stages) → `../foundations/key-definitions.qmd`

---

## Later-on analysis
Extensions: multiple crime types (substitution), heterogeneous offenders (concentration), reporting stages, dynamic learning.
